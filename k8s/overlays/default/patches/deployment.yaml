apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: lootopia-frontend
          envFrom:
            - configMapRef:
                name: app-config


